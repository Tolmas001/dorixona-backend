app = "dorixona-backend"
primary_region = "iad"

[build]
dockerfile = "Dockerfile"

[deploy]
healthcheck_path = "/health"

[[services]]
internal_port = 3004
protocol = "tcp"

  [[services.ports]]
  handlers = ["http"]
  port = 80

  [[services.ports]]
  handlers = ["tls", "http"]
  port = 443
